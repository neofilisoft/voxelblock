# VoxelBlock Tools

Python tooling scripts for automation, asset processing, scene utilities, and export helpers.

Suggested uses:
- asset validation / batch import
- scene JSON generation
- export packaging helpers
- smoke tests for project structure

Start with `voxelblock_tool.py` and extend per workflow.
